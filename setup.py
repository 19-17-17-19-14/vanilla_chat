from setuptools import find_packages, setup

setup(
    name = 'Vanilla Chat',
    version = '0.0.0',
    author = 'me',
    author_email = 'my.email@fake_email_address_yall.com',
    packages = find_packages(),
    install_requires = []
)